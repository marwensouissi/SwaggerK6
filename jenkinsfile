pipeline {
    agent any

    environment {
        BITBUCKET_REPO = "https://bitbucket.org/ndammak/itona-k6.git"
        BITBUCKET_CRED_ID = 'bbck'
        K6_WEB_DASHBOARD = "true"
        XK6_PATH = "/var/lib/jenkins/go/bin/xk6"  // Ensure this path is correct
        WORKSPACE_DIR = "${env.WORKSPACE}"
    }

    stages {

        stage('Checkout Code') {
            steps {
                script {
                    echo "🔄 Fetching code from Bitbucket..."
                    git branch: 'main',
                        credentialsId: BITBUCKET_CRED_ID,
                        url: BITBUCKET_REPO
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                script {
                    sh '''
                    echo "⚙️ Installing K6 and dependencies..."
                    go install go.k6.io/xk6/cmd/xk6@latest
                    docker pull grafana/xk6
                    docker pull eclipse-mosquitto
                    '''
                }
            }
        }

        stage('Build xK6 with MQTT and File Plugins') {
            steps {
                script {
                    sh '''
                    echo "🔨 Building xK6 with MQTT and File plugins..."
                    $XK6_PATH build v0.54.0 \
                        --with github.com/pmalhaire/xk6-mqtt@latest \
                        --with github.com/avitalique/xk6-file@latest

                    mv k6 $WORKSPACE/k6  # Move compiled K6 binary to workspace
                    chmod +x $WORKSPACE/k6  # Ensure it's executable
                    '''
                }
            }
        }

        stage('Run K6 Device Test') {
            steps {
                script {
                    sh '''
                    echo "📡 Running K6 device_test.js scenario..."
                    cd final
                    $WORKSPACE/k6 run --duration 1m --vus 3 final/device_test.js
                    '''
                }
            }
        }

        stage('Fix JSON Output') {
            steps {
                script {
                    sh '''
                    echo "🛠️ Fixing JSON output with Node.js formatter..."
                    node JsonFormatter
                    '''
                }
            }
        }

        stage('Run MQTT Connection Test') {
            steps {
                script {
                    sh '''
                    echo "🔗 Running MQTT connection test..."
                    $WORKSPACE/k6 run --duration 1m --vus 3 final/Mqtt_connection.js
                    '''
                }
            }
        }
    }

    post {
        always {
            echo "✅ Pipeline execution completed!"
        }
        success {
            echo "🎉 All K6 and MQTT Tests Passed!"
        }
        failure {
            echo "❌ Some tests failed!"
        }
    }
}
